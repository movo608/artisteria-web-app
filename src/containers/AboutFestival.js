import React, { Component } from 'react'
import { connect } from 'react-redux'

class AboutFestival extends Component {
	render() {
		return (
			<div>AboutFestival</div>
		);
	}
}

export default connect()(AboutFestival);